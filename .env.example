# ============================================================
# Football Prediction System - Environment Variables
# ============================================================
# 复制此文件为 .env 并填入实际值
# cp .env.example .env
# ============================================================

# ==================== 通用配置 ====================
NODE_ENV=development
# NODE_ENV=production  # 生产环境

# ==================== 数据库配置 ====================
# 本地开发
POSTGRES_USER=football_user
POSTGRES_PASSWORD=football_pass
POSTGRES_DB=football_db
DATABASE_URL=postgresql://football_user:football_pass@localhost:5432/football_db

# Railway PostgreSQL (部署后自动设置)
# DATABASE_URL=postgresql://postgres:xxx@xxx.railway.app:5432/railway

# ==================== Redis 配置 ====================
# 本地开发
REDIS_URL=redis://localhost:6379

# Railway Redis (部署后自动设置)
# REDIS_URL=rediss://default:xxx@xxx.railway.app:6379

REDIS_CHANNEL_MATCH_EVENTS=match_events
REDIS_CHANNEL_PREDICTIONS=predictions

# ==================== 后端服务配置 ====================
# 端口 (Railway 会自动设置 PORT 环境变量)
PORT=4000

# 数据模式: 'live' 使用真实 API, 'mock' 使用模拟数据
DATA_MODE=mock

# JWT 密钥 (生产环境请更换)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# ==================== 预测服务配置 ====================
ENVIRONMENT=development
DEBUG=true
MODEL_VERSION=v1.0.0
MODEL_PATH=/app/ml/models

# 是否启用数据库写入
DB_ENABLED=false

# ==================== 外部 API 配置 ====================
# API-Football (https://www.api-football.com/)
API_FOOTBALL_KEY=your-api-key-here
API_FOOTBALL_URL=https://v3.football.api-sports.io

# 轮询间隔（秒）- 建议不低于 15 秒
POLL_INTERVAL=15

# ==================== CORS / 前端配置 ====================
# 前端 URL (用于 CORS 白名单)
# 本地开发
FRONTEND_URL=http://localhost:5173

# Vercel 部署后
# FRONTEND_URL=https://your-app.vercel.app

# 前端环境变量
VITE_API_URL=http://localhost:4000
VITE_WS_URL=ws://localhost:4000

# ==================== Railway 特定配置 ====================
# Railway 会自动设置以下变量，无需手动配置：
# - PORT: 服务端口
# - RAILWAY_ENVIRONMENT: 环境名称
# - RAILWAY_PROJECT_ID: 项目 ID
# - RAILWAY_SERVICE_ID: 服务 ID
